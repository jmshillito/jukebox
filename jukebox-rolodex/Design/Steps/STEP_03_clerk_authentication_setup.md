# Step 3: Clerk Authentication Setup


Create a Clerk application.

You need:
- Publishable key (frontend)
- Secret key (server)
- JWT Verification Key (public key)

You will:
1. Add Clerk script to index.html
2. Mount sign-in UI
3. Request a session token in JS
4. Send that token as `Authorization: Bearer <token>` to /api calls

